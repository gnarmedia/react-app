/**
 * @file Home component
 */

import React from "react";

/**
 * Renders Home component.
 *
 * @example
 * <Home />
 *
 * @returns {Function} Home component.
 */
const Home = () => (
  <>
    <section className="hero">
      <div className="hero-body">
        <h1 className="title">Welcome to React with serverside rendering!</h1>
        <h2 className="subtitle">...now to add all the other stuff...</h2>
      </div>
    </section>
  </>
);

export default Home;
